steps:
  # Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/renewaa-420310/renewaa", "."]

  # Push the Docker image to Google Container Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/renewaa-420310/renewaa"]

  # Deploy the application and database with docker compose
  - name: "gcr.io/cloud-builders/docker"
    args: ["compose", "-f", "docker-compose.yaml", "up", "-d"]
#  # Deploy the Docker image to the Google Cloud Run service
#  - name: "gcr.io/cloud-builders/gcloud"
#    args:
#      - "run"
#      - "deploy"
#      - "my-service"
#      - "--image"
#      - "gcr.io/renewaa-420310/renewaa"
#      - "--region"
#      - "us-central1"
#      - "--platform"
#      - "managed"
